apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cert-practice-cloudflared-credentials
  namespace: default
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: azure-keyvault
    kind: ClusterSecretStore
  target:
    name: cert-practice-cloudflared-credentials
    creationPolicy: Owner
  data:
    - secretKey: credentials.json
      remoteRef:
        key: prod-cert-practice-cloudflare-credentials
