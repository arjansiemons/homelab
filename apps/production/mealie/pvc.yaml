kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: mealie-api-data
  namespace: default
spec:
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: 1Gi
  storageClassName: "proxmox-csi-nvme"
