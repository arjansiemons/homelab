apiVersion: v1
kind: Service
metadata:
  name: mosquitto-p
  namespace: default
spec:
  ports:
    - name: mqtt
      port: 1883
      targetPort: mqtt
  selector:
    app: mosquitto
    type: primary