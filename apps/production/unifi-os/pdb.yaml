apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: unifi-os
  namespace: unifi-os
spec:
  maxUnavailable: 0
  selector:
    matchLabels:
      app.kubernetes.io/instance: unifi-os-server
      app.kubernetes.io/name: unifi-os-server
