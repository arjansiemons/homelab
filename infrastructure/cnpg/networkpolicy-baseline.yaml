apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: postgresql-system-baseline
  namespace: postgresql-system
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - {}
  egress:
    - {}
