apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: proxmox-csi-nvme
mountOptions:
  - noatime
parameters:
  cache: writethrough
  csi.storage.k8s.io/fstype: ext4
  ssd: "true"
  storage: nvme-k8s
provisioner: csi.proxmox.sinextra.dev
reclaimPolicy: Delete
volumeBindingMode: WaitForFirstConsumer
allowVolumeExpansion: true
